org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Jetifier rewrites legacy com.android.support libraries to AndroidX. Nothing
# in this project uses them, and the transform is slow and memory-hungry — it
# exhausted the heap on CI runners. Off unless a support-library dependency
# reappears.
android.enableJetifier=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
